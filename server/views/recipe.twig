{% extends 'layout.html' %}

{% block content %}
    <h1>{{ recipe.name }}</h1>
    <ul class='ingredients'>
    {% for ing in ingredients %}
    <li class='user'>
        <p>{{ composed.quantity + ' ' + composed.unity + ing.id + ' ' + ing.name }}</p>
    </li>
    {% endfor %}
    </ul>
{% endblock %}
