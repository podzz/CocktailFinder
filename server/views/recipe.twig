{% extends 'layout.html' %}

{% block content %}
    <h1>{{ recipe.name }}</h1>
    <ul class='ingredients'>
    {% for ing in ingredients %}
    <li class='user'>
        <p><a href="">{{ composed[loop.index - 1].quantity }} {{ composed[loop.index - 1].unity }}</a>
        {{ ing.name }}</p>
    </li>
    {% endfor %}
    </ul>
{% endblock %}
